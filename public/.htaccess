Options -MultiViews
RewriteEngine On
RewriteBase /

# Redirige todo al index.html salvo que sea un archivo o carpeta real
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
